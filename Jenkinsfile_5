pipeline {
  agent any

  stages {
    stage ("Test") {
      steps {
        echo " echo keyword is used to print message"
        dir ("Sameple") {
          echo "It will print current directory"
          echo pwd()
        }
        
        
      }
    }
  }
}
